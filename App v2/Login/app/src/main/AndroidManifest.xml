<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ideapad.login">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="Inicio de Sesión">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Lista"
            android:label="@string/title_activity_lista"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->


        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCbsj88m0rfFYYk_CG-xxe5FmBg349ZYTc" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".ubicacion"
            android:label="@string/title_activity_ubicacion" />

        <activity android:name=".Menu"
            android:label="Menú principal"/>

        <activity android:name=".Formulario" />
        <activity android:name=".Lista_datos_manuales" />
        <activity android:name=".Lista_datos_sensor" />
        <activity android:name=".ListaT_sensor" />
        <activity android:name=".Conectar_sensor" />

        <activity android:name=".Lista_predio"
            android:label="Lista de predios"/>
        <activity android:name=".Lista_tareas_m"
            android:label="Lista de tareas manuales"/>
        <activity
            android:name=".Mapa_ubicacionesT"
            android:label="Puntos de ingreso de datos" />
        <activity android:name=".Form_tarea"
            android:label="Formulario de ingreso"/>
        <activity android:name=".Lista_predioTS"
            android:label="Lista de predios"/>
        <activity android:name=".Lista_tareaS"
            android:label="Lista de tareas con sensores"/>

        <activity android:name=".Rastreador_sensor" />
        <activity android:name=".Visualizacion_datos" />
        <activity
            android:name=".Mapa_ubicacionesS"
            android:label="Ubicaciones de sensores" />

        <activity
            android:name=".Mapa_ubicacionS2"
            android:label="@string/title_activity_mapa_ubicacion_s2" />

        <activity android:name=".Lista_fechas"
            android:label="Lista de fechas"/>

        <activity android:name=".Lista_grafico" />

        <activity android:name=".Grafico"
            android:label="Gráfico de humedad"/>
        <activity android:name=".Lista_datos_sensores"
            android:label="Datos obtenidos de los sensores">
        </activity>
    </application>

</manifest>